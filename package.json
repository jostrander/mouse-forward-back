{
  "name": "mouse-forward-back",
  "version": "1.0.0",
  "description": "A module used to listen for buttons 8/9 for a window in Electron.",
  "main": "mouse-forward-back.js",
  "gypfile": true,
  "license": "MIT",
  "os": ["linux"],
  "scripts": {
    "install": "node-gyp rebuild",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "bindings": "^1.2.1",
    "nan": "*"
  }
}
